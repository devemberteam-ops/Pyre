# Bugs encontrados - onda de de-risk 1.1.3

> Caca multi-agente (8 fatias disjuntas) + verificacao adversaria. 22 confirmados de 26 candidatos.
> Status: AGENT-CONFIRMED. CEO le o codigo real dos HIGH/CRITICAL antes de virar fix.

## HIGH (3)

### [creator] Character/persona EDIT silently DROPS a prose-field paragraph when a later paragraph starts with `Word:`
- **Arquivo:** lib/services/creator_render.dart:425
- **Impacto:** Permanent, silent data loss on a character/persona edit: any multi-paragraph prose field whose non-first paragraph opens with `Capitalized…:` (a very common prose shape — "His motto:", "Later:", "Note:", "In short:") loses that paragraph the moment the user runs any edit build. The user asked to change field X and field Y quietly loses content.
- **Repro:** Build (or import) a character whose Background (or any prose Description field) has a paragraph break followed by a line that starts with a capitalized word + colon, e.g. Background = "He fled the capital after the coup.\n\nLater: he resurfaced as a smuggler." The renderer emits it verbatim as `Background: He fled...\n\nLater: he resurfaced...`. Now open the card in the Creator to EDIT it and ask for any unrelated change. `_runStructuredBuildFlow` (character_assistant_screen.dart:827) calls `decomposeDescription`→`_decomposeLabeled`, whose blank-line invariant (creator_render.dart:417-436) treats the blank-line-preceded `Later:` line as a NEW top-level label, splitting that paragraph OUT of `background` into a foreign key `Later`. That foreign key lives in `existing`, but for character/persona edits `buildFields = fields` (character_assistant_screen.dart:987-991) — foreign-keyed fragments are NEVER carried into the rebuilt field map (only scenario `#N` dup tags are, via carryForwardDuplicateTags). So `renderCard(fields)` re-emits a `background` missing that paragraph and the fragment is permanently lost.
- **Por que e real:** Reachable through the shipped Edit-with-AI flow. `_kTopLabelRe` (creator_render.dart:501) matches `^Word:` and the only guard (line 425) is "previous line is blank", which is exactly true at a paragraph break. The split fragment is keyed foreign and `_looksForeignLabel` (line 754) returns true for an uppercase-leading word, so it is re-emitted as a standalone `Label: …` topic on a non-edit round-trip — but in the EDIT path it is dropped because foreign keys from `existing` are not merged into the model's `fields` before `renderCard`.
- **Veredito adversario:** CONFIRMED real after trying hard to refute. I traced the full edit path and reproduced the loss empirically with a probe test against the actual code.

Mechanism (all verified in source, not assumed):
1. renderCard prose emission (creator_render.dart:246-253) writes a multi-paragraph prose value VERBATIM, preserving internal blank lines. So background="He fled the capital after the coup.\n\nLater: he resurfaced as a smuggler." renders as `Background: He fled...\n\nLater: he resurfaced...`, and topics are joined with `\n\n`, so the `Later:` line is preceded by a blank line in the final Description.
2. _decomposeLabeled's I1 blank-line invariant (creator_render.dart:417-436, esp. the guard at 

### [sync-server] fullResync() silently no-ops when a periodic tick is in flight — newly-enabled key-sync pulls nothing
- **Arquivo:** lib/services/sync_engine.dart:216
- **Impacto:** Turning on provider-key sync appears to do nothing — the user sees the 'Pulling providers & keys from the PC…' snackbar but the API keys/providers never arrive on the phone, with no error. This is the exact flow fullResync was built for (per its own doc comment), and it is defeated by an overlapping background tick.
- **Repro:** 1. Pair a phone to the desktop. 2. Let the app run so the 30s periodic poll (or an app-resume / LanClient-change tick) is mid-flight (each tick spans real HTTP round-trips during which `_tickInFlight==true`). 3. In LAN Connect, flip the 'Sync provider keys' switch ON. lan_connect_screen.dart:458-465 calls store.setSyncProviderKeys(v) then `await SyncEngine.instance.fullResync()`. fullResync() (sync_engine.dart:216) sets `_lastServerTime=0`, persists 0 to prefs, then calls forceTick()->_tick(). _tick() hits `if (_tickInFlight) return;` (line 318) and returns immediately WITHOUT pulling. The already-in-flight tick, which captured/uses `_lastServerTime` and at its tail does `_lastServerTime = serverTime` + persists it (lines 1164-1168), overwrites the just-zeroed watermark with the server's current time. The full re-pull from since=0 never happens; providers (stamped at desktop launch, i.e. BELOW the advanced cursor) are never fetched.
- **Por que e real:** fullResync() does not guard against or wait out an in-flight tick, and forceTick()->_tick() unconditionally early-returns on `_tickInFlight`. The periodic poll (Timer.periodic every 30s, sync_engine.dart:303) plus resume/onLanClientChange ticks make an overlap likely during normal use. The in-flight tick's terminal `_lastServerTime = serverTime` (line 1164) clobbers the zeroed watermark, so the intended since=0 re-pull is permanently lost (the cursor is now past the providers' mtime).
- **Veredito adversario:** I tried to refute this and could not. The claim is accurate and I reproduced its worst case with a faithful event-loop simulation of the exact field/prefs read-write sequence.

Code verified (lib/services/sync_engine.dart):
- fullResync() (216-223): sets `_lastServerTime = 0`, persists 0 to prefs, then `await forceTick()` -> `_tick()`. It has NO guard against an in-flight tick and never waits one out.
- `_tick()` (314-320): `if (_tickInFlight) return;` is synchronous and BEFORE any await; an overlapping call early-returns without pulling. `_tickInFlight` is the documented re-entry guard whose stated contract ("the next timer picks up the slack", line 35-38) is only valid for idempotent perio

### [presets-memory] Editing a modular preset silently strips every block's injection depth on Save
- **Arquivo:** lib/screens/presets_screen.dart:916
- **Impacto:** Permanent, silent corruption of the preset's prompt structure. assemblePreset (preset_assembly.dart:82) routes `depth != null` blocks into depthTurns, which insertDepthTurns (chat_prompt_builder.dart:166) splices mid-history at the requested depth; with depth lost the block instead lands as post-history text — a jailbreak/reminder that was injected N turns from the end now appears in a different position every turn, changing model behavior. It is unrecoverable through the UI: once the draft has depth:null, reopening the block editor reads block.depth==null so it can't restore it.
- **Repro:** 1. Import a SillyTavern preset that contains an in-chat injection prompt (`injection_position: 1`) — e.g. a jailbreak/reminder. st_preset_import.dart sets that block's `depth` (default 4). 2. Open Presets → tap that preset's kebab → Edit. 3. Tap Save (no edits needed). The editor builds its working `blocks` draft at lines 914-924 with `PromptBlock(id, name, content, enabled, role, position)` — `depth` is OMITTED, so every depth block in the draft becomes `depth: null`. On Save, line 1160 commits `existing.promptBlocks = blocks`, overwriting the real blocks. The depth-4 mid-history injection is now an afterHistory (post-history) block.
- **Por que e real:** PromptBlock has a first-class `int? depth` field (models.dart:1212) that round-trips through JSON (1231/1241) and is honored by assembly + the prompt builder (verified). The block-add (presets_screen.dart:1266) and ST import (st_preset_import.dart:231) correctly populate depth; only the two copy sites (509 clone, 916 edit-draft) drop it, proving the omission is a mistake. The edit-draft path requires zero special interaction — a plain Save corrupts it.
- **Veredito adversario:** CONFIRMED after trying to refute it. The edit-draft at lib/screens/presets_screen.dart:914-924 deep-copies each PromptBlock with only {id,name,content,enabled,role,position} and OMITS the first-class `int? depth` field (models.dart:1212), so every block in the working draft becomes depth:null the moment the Edit dialog opens. On Save, _editPreset (line 881) unconditionally commits `existing.promptBlocks = blocks` (line 1160) and updatePreset (app_store.dart:2990) persists it (and bumps mtime → propagates over sync).

Every refutation angle failed:
- depth IS load-bearing: assemblePreset (preset_assembly.dart:82-84) routes depth!=null blocks into depthTurns (mid-history splice); without depth

## MEDIUM (7)

### [chat-runtime] Native Anthropic streaming/one-shot paths skip the universal unsupported-param 400 retry and safeBodyFor, so a single rejected param hard-fails the whole turn
- **Arquivo:** lib/services/chat_api.dart:536
- **Impacto:** On Anthropic providers a single stray extraParam (very plausible after the user duplicates/edits a provider row, or via synced provider records) dead-ends every generation with a raw 4xx instead of the fail-soft retry every OpenAI-format provider enjoys. The user just sees errors with no path to a working reply, even though the minimal-safe body would have succeeded.
- **Repro:** Use an Anthropic-format provider with provider.extraParams carrying any field Anthropic rejects (e.g. a leftover `frequency_penalty`, `response_format`, or a renamed sampling field synced/imported from an OpenAI provider row), or a stop sequence Anthropic dislikes. _streamAnthropic (line 504-512) builds the body via buildAnthropicBody — which never calls safeBodyFor — and on a 4xx (line 536-542) it throws immediately with no isUnsupportedParamError/minimalRetryBody retry. The OpenAI streaming path (lines 838-861) and completeChat (lines 1275-1295) both have the universal 'retry-without-extras exactly once' backstop.
- **Por que e real:** The OpenAI paths were explicitly hardened with the 'THE key fix' universal param-error retry (comments at lines 831-838, 1270-1275); the Anthropic adapter, added later in 1.1.3, reuses the HTTP machinery but omits both safeBodyFor and the 400-retry entirely (verified: _streamAnthropic line 536 and _completeAnthropic line 637 both throw on >=400 with no retry branch). extraParams ARE spread into the Anthropic body (buildAnthropicBody line 433), so a rejected param reaches the wire and cannot be recovered.
- **Veredito adversario:** Verified the claim against the actual code and Anthropic API behavior; I could not refute it.

CODE TRACE (lib/services/chat_api.dart):
- buildAnthropicBody (line 432-439) spreads `...?extraParams` directly into the /v1/messages body; only Pyre-managed keys (model/max_tokens/system/messages/stream/temperature/top_p/top_k/stop_sequences) override collisions. Any non-managed extra field (frequency_penalty, response_format, reasoning_effort, include_reasoning, enable_thinking) survives to the wire.
- _streamAnthropic (line 504-512) and _completeAnthropic (line 605-613) both pass `provider.extraParams` and call buildAnthropicBody — never safeBodyFor.
- On a 4xx, _streamAnthropic (536-542) and _c

### [creator] Trailing-comma cleanup regex is not string-aware and silently corrupts string VALUES containing `,}` / `,]`
- **Arquivo:** lib/services/creator_json.dart:33
- **Impacto:** A generated card field is silently changed from what the model produced — characters dropped from a description/kink/notes value. Always runs (the regex is applied to every balanced object even when there were no real trailing commas), so it can corrupt otherwise-valid JSON, not just malformed JSON.
- **Repro:** A batch JSON value contains a `,` immediately followed (optionally by whitespace) by `}` or `]` inside a string, e.g. the model returns {"notes":"items in his bag: rope, } a knife"} or any value ending "...trailed off, }". `extractJsonObject` runs `result.replaceAllMapped(RegExp(r',(\s*[}\]])'), (m)=>m.group(1)!)` on the raw balanced substring BEFORE `jsonDecode`, deleting the comma inside the string. The decode then succeeds but the stored field value has been altered (`rope, }` → `rope }`).
- **Por que e real:** The regex operates on the unparsed text with no in-string tracking (unlike the sibling `_escapeControlCharsInStrings`/`_scanState`, which ARE string-aware). The comment claims it's "safe simple regex for well-formed JSON" but it cannot distinguish structural commas from in-string commas. Reachable whenever a model writes a value containing `,}`/`,]`; the corrupted-but-valid result is then rendered into the card with no error surfaced.
- **Veredito adversario:** CONFIRMED real and reproducible by running the actual extractJsonObject (lib/services/creator_json.dart:24-53) via the Dart SDK. The trailing-comma cleanup at creator_json.dart:33-36 -- result.replaceAllMapped(RegExp(r',(\s*[}\]])'), (m)=>m.group(1)!) -- is applied to the raw balanced substring with NO in-string tracking, unlike the string-aware sibling _escapeControlCharsInStrings/_scanState. The brace scanner correctly returns the WHOLE valid object (it IS string-aware), then this naive regex mutates a string VALUE that happens to contain a comma followed (optionally by whitespace) by a literal } or ]. Verified outputs: {"notes":"items in his bag: rope, } a knife"} -> stored value "items i

### [creator] JSON-continuation seam corrupts the stitch when a misbehaving model re-emits the whole object and the partial broke OUTSIDE a string
- **Arquivo:** lib/services/creator_build.dart:157
- **Impacto:** That batch yields no parseable object, so its fields come back empty; after the bounded missing-key re-request also fails, the card is built with that whole batch's fields blank (surfaced only as a soft 'missing' note). Best-effort fallback limits blast radius, but a recoverable response is discarded.
- **Repro:** A batch reply truncates right after a complete element but with the object still open, e.g. `{"a":"x",` (depth>0, NOT inside a string). `looksTruncatedJson` is true so the continuation fires; a poorly-behaved model, told to "continue", re-emits the whole object from scratch: `{"a":"x","b":"y"}`. The seam guard at creator_build.dart:157 (`endsInsideUnterminatedString(raw)`) is FALSE here (the break was outside a string), so the standalone-object branch is skipped and `raw += more` produces `{"a":"x",{"a":"x","b":"y"}` → `extractJsonObject` scans the outer braces and fails to decode.
- **Por que e real:** The MEDIUM-8 seam guard only handles the mid-string break case; the equally-common mid-object-but-outside-string break (very typical of length-cut JSON, which stops after `",`) is not covered, and a from-scratch re-emit is an explicitly anticipated model misbehavior (the guard comment names it). Lower confidence on frequency, but the corrupting concat path is real and unguarded for this case.
- **Veredito adversario:** CONFIRMED REAL after trying to refute it. I exercised the actual functions and the live pipeline (not just reading): for a partial that breaks outside a string (e.g. `{"a":"x",`), looksTruncatedJson is true (continuation fires) but endsInsideUnterminatedString is false (creator_build.dart:157 seam guard skipped), so the code does `raw += more`. When a misbehaving model re-emits the whole object, the stitch becomes `{"a":"x",{"a":"x","b":"y"}` and extractJsonObject returns null. Running the real runStructuredBuild end-to-end with this exact sequence returns `{}` — the ENTIRE batch's fields are lost, and it burns all continuation calls (the corrupted buffer keeps looksTruncatedJson true since 

### [sync-server] /bbx/ reverse-proxy is an UNAUTHENTICATED outbound-fetch endpoint usable by any LAN device
- **Arquivo:** lib/services/pyre_server.dart:981
- **Impacto:** Without pairing, an unauthenticated LAN peer can drive arbitrary GET/POST requests to botbooru.com through the user's machine/IP (request laundering / SSRF-to-a-fixed-host, traffic amplification), and consume the host's bandwidth on demand. Unlike every other sync route, no bearer is required.
- **Repro:** Start the LAN server with the default 'Entire LAN' bind (InternetAddress.anyIPv4, pyre_server.dart:120). The route `r.all('/bbx/<rest|.*>', _proxyBotbooru)` is registered (line 981) but `/bbx/` is NOT in `_protectedPrefixes` (lines 1619-1629: only /pull,/push,/llm/,/attachments,/manifest), so `_authMiddleware` (line 1638 `_protectedPrefixes.any(path.startsWith)`) treats it as public. Any host on the LAN can issue `GET http://<pc-ip>:<port>/bbx/<anything>?<query>` (or POST/PUT/PATCH, which forward a body — lines 1008-1015) with NO bearer; _proxyBotbooru fetches `https://botbooru.com/<rest>?<query>` from the desktop's IP and returns the bytes.
- **Por que e real:** The auth deny-list omits the /bbx/ prefix and the route is registered unconditionally for every running server. The default UI bind is entireLan over plaintext HTTP (main.dart:243-249), so the endpoint is reachable by anyone on the same network with no credential. The target host is hardcoded to botbooru.com (not a fully open relay), but the unauthenticated outbound-fetch + bandwidth-drain surface is real and reachable.
- **Veredito adversario:** I tried hard to refute this and could not. The code reads exactly as claimed. lib/services/pyre_server.dart:981 registers `r.all('/bbx/<rest|.*>', _proxyBotbooru)` unconditionally inside `_router` (the single router mounted on the LAN pipeline at line 158 — no kIsWeb/feature-flag guard, so it is reachable on native desktop even without a web build). `/bbx/` is absent from `_protectedPrefixes` (lines 1619-1629: only /pull,/push,/llm/,/attachments,/manifest), so `_authMiddleware` (line 1638 `_protectedPrefixes.any(path.startsWith)`) computes needsAuth=false and calls `inner(req)` with no bearer. `_proxyBotbooru` (lines 996-1037) then forwards the method + path (`rest`) + query and, for POST/PU

### [import-export] Lorebook import throws TypeError on off-spec field types, aborting the whole card/lorebook import
- **Arquivo:** lib/services/lorebook_import.dart:161
- **Impacto:** A valid character whose card carries a foreign/off-spec embedded lorebook (or a standalone lorebook with int-as-bool flags) cannot be imported at all — the import dies with an opaque TypeError. This defeats the function's explicit contract ('Tolerates partial / off-spec shapes — ST and Chub variants disagree on field names'). Note the inconsistency: enabled/disable/constant/useProbability use the safe `is bool ?` pattern, but caseSensitive/matchWholeWords/content/order/probability use hard casts.
- **Repro:** Import any chara_card_v2 PNG/JSON (or standalone lorebook) whose character_book has an entry with a non-canonical field type — e.g. SillyTavern/older exporters commonly emit caseSensitive/matchWholeWords as 0/1 integers instead of JSON booleans, or content/insertion_order as a number/string. lorebookFromCharacterBook does raw casts: `m['content'] as String?` (L161-162), `m['insertion_order']/['priority']/['order'] as num?` (L174-176), `m['caseSensitive']/['matchWholeWords'] as bool?` (L188-189), `m['probability'] as num?` (L190). A wrong-typed value throws `_TypeError` (e.g. "type 'int' is not a subtype of type 'bool?'"). The per-entry loop has no try/catch, so the exception propagates out of lorebookFromCharacterBook. For an embedded book this kills handleEmbeddedBookForCharacter, which is awaited inside every character-import path (_pickAndImportCard, _doImportCharacterBytes, main.dart ?import=) → the ENTIRE import fails with 'Import failed: TypeError' even though the character card itself is perfectly valid. For a standalone book the Discover/lorebook-file import fails with the same ugly error.
- **Por que e real:** Confirmed by reading the code: lines 161-190 use raw `as` casts with no surrounding try/catch in the entry loop, while the codebase elsewhere (models.dart) uses tolerant _jInt/_jStringList helpers. caseSensitive/matchWholeWords-as-0/1 is a real-world SillyTavern/export shape, and `0 as bool?` throws in Dart. tryParseLorebookJson only wraps the NATIVE Lorebook.fromJson branch in try/catch (L270-283), NOT the lorebookFromCharacterBook branches (L290/295/304), so the throw escapes to the caller's generic catch.
- **Veredito adversario:** Verified, not refuted. (1) Dart cast semantics confirmed by running a real program via the project's Dart SDK: `0 as bool?`, `123 as String?`, `"5" as num?`, `"high" as num?` all throw _TypeError; only null is safe. (2) End-to-end through the actual source: a probe calling tryParseLorebookJson with an ST-standalone book carrying caseSensitive:0 threw `_TypeError: type 'int' is not a subtype of type 'bool?'` and the exception propagated OUT of the function — confirming there is no entry-loop try/catch and that tryParseLorebookJson wraps only the native-shape branch (lorebook_import.dart:266-283), not the lorebookFromCharacterBook branches (L290/295/304). (3) Blast radius confirmed: characters

### [import-export] Export as PNG card fails when the avatar is a JPEG/WEBP/GIF (e.g. a gallery image set 'Use as avatar')
- **Arquivo:** lib/screens/characters_screen.dart:653
- **Impacto:** Exporting/sharing/uploading a character (or persona — _exportPersonaAsPng has the identical path at L774) as a chara_card_v2 PNG is completely broken for any card whose avatar is a non-PNG image, with a confusing error and no guidance. The avatar still displays fine in the app, so the user has no idea why export refuses.
- **Repro:** Add a non-PNG image to a character's gallery (gallery_editor_section.dart uses FileType.image → allows .jpg/.webp/.gif and stores the raw bytes with their real mime, NO PNG re-encode), then tap 'Use as avatar' (character_edit_screen.dart L538-545 sets `_avatar = _gallery[i]` directly). Save, then choose 'Export as PNG card'. _exportCharacterAsPng calls resolveAvatarBytes(c.avatar) which returns the raw JPEG/WEBP bytes, then encodeCharaCardPng(c, avatarBytes) → parsePngChunks(avatarBytes) throws FormatException('Not a PNG (bad signature)'). The catch shows 'Export failed: ...Not a PNG...'.
- **Por que e real:** Traced end to end: gallery picker stores original bytes (gallery_editor_section.dart L57-69, mime can be image/jpeg|webp|gif via _imageMime in gallery_import.dart), 'Use as avatar' repoints _avatar with no conversion (character_edit_screen.dart L538-545), and encodeCharaCardPng/parsePngChunks hard-require a PNG signature (png_parser.dart L77-82). _exportCharacterAsPng only null-checks avatarBytes (L645), never validates it is a PNG, so the FormatException is reachable and surfaces as 'Export failed'.
- **Veredito adversario:** CONFIRMED after exhausting every refutation path. The bytes flow unguarded end-to-end:

1. Gallery picker (lib/widgets/gallery_editor_section.dart:57-69) uses FileType.image (accepts jpg/webp/gif) and stores the RAW picked bytes with their real mime — no PNG re-encode.
2. AttachmentStore.store (lib/services/attachment_store.dart:90-113) writes bytes VERBATIM (writeAsBytes(bytes)); it is pure content-addressed storage with zero format conversion. gallery_import.dart:88 confirms image/jpeg is a first-class, preserved mime.
3. "Use as avatar" (lib/screens/character_edit_screen.dart:538-545) sets `_avatar = _gallery[i]` — a plain ref copy, no conversion.
4. Save → _composeFromForm (character_edi

### [state-models] Attachment GC reaps avatar/gallery blobs referenced only by per-chat character snapshots → broken avatars
- **Arquivo:** C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\services\attachment_refs.dart:71
- **Impacto:** collectReferencedAttachmentHashes (attachment_refs.dart:71-119) scans s.characters, s.personas, s.characterDrafts, chatSettings/per-chat backgrounds and the botbooru avatar, but NEVER iterates chat.characterSnapshots. Once the library character is gone, its avatar/avatarOriginal/gallery hashes are absent from the referenced set, so gcOrphans deletes those .bin files (the 120s mtime grace only protects freshly-written blobs, not an old avatar). The chat's snapshot — which is consulted FIRST for rendering (chat_screen.dart:450/893, chat_tree_screen.dart:190, group_lorebooks_sheet.dart:152) — then renders a broken/placeholder avatar permanently. Group chats make this routine: addCharacterToChat (app_store.dart:2788) freezes a snapshot too, and removing a member from the library is normal cleanup.
- **Repro:** 1) Start a chat with a character whose avatar is a pyre://attachment ref (the normal native case). startChatWith (app_store.dart:2527) freezes Character.fromJson(character.toJson()) into chat.characterSnapshots, copying the avatar/avatarOriginal/gallery refs. 2) Delete that character from the library (removeCharacter, app_store.dart:1892 — it deliberately does NOT delete the chat or its snapshot). 3) Relaunch the app (or have the LAN server running). The once-per-launch sweep AppStore.load() line 961 calls AttachmentStore.gcOrphans(collectReferencedAttachmentHashes(this)).
- **Por que e real:** Verified that startChatWith and addCharacterToChat both store a full Character snapshot including the avatar ref; that removeCharacter intentionally keeps the chat+snapshot; that collectReferencedAttachmentHashes (the SINGLE referenced-set used by BOTH GC triggers — local sweep app_store.dart:961 and PyreServer._runAttachmentGc pyre_server.dart:200) omits chat.characterSnapshots; and that snapshots are the primary avatar source in the render paths. A grep of lib/services confirms nothing else adds snapshot avatars/galleries to the GC set.
- **Veredito adversario:** Verified end-to-end and could not refute. (1) startChatWith (app_store.dart:2527) and addCharacterToChat (app_store.dart:2788) freeze Character.fromJson(character.toJson()) into chat.characterSnapshots; Character.toJson (models.dart:471-474) preserves the pyre://attachment avatar + avatarOriginal refs, which are the normal native case. (2) removeCharacter (app_store.dart:1892) deletes only from `characters` + records a tombstone, never the chat/snapshot, and the load-time sweep (app_store.dart:1057-1061) deliberately KEEPS snapshot-only characters, so the snapshot survives forever. (3) collectReferencedAttachmentHashes (attachment_refs.dart:71-119) scans characters/personas/drafts/background

## LOW (12)

### [chat-runtime] Anthropic adapter folds ALL system turns to the top `system` field, hoisting post-history jailbreak, story roadmap, guide note, and mid-chat scene/system messages out of their intended position
- **Arquivo:** lib/services/chat_api.dart:388
- **Impacto:** For Anthropic providers the post-history jailbreak/reminder — which is deliberately the LAST thing the model reads (closest to generation focus) on OpenAI providers — is collapsed into the top system block mixed with the character card, materially weakening jailbreak strength and 'stay in character' reminders. The Story Roadmap and one-shot Guide note lose their 'after history' placement, and a mid-conversation Scene/system message is hoisted to always-true global context instead of marking a transition at its point in the timeline. Output quality/steering degrades on exactly the high-value Claude routes the 1.1.3 adapter was built for.
- **Repro:** Configure an Anthropic-format provider (provider.format == ApiFormat.anthropic, the 1.1.3 native /v1/messages adapter). Use a preset with post-history instructions (the char-voice jailbreak), and/or enable the Story Roadmap or a Guide note, and/or insert a Scene message mid-chat. Send a turn. buildChatPrompt emits these as ChatTurn('system', ...) turns placed AFTER the conversation (post-history at the very end, roadmap after history, scene at its chronological index). buildAnthropicBody (lines 388-396) then iterates every message and folds EVERY m.role=='system' into systemParts, which is join('\n\n')-ed into the single top-level `system` field at line 436.
- **Por que e real:** buildAnthropicBody unconditionally moves every system-role turn to the top `system` string (line 390-396) with no preservation of trailing/mid-stream system turns as user/assistant turns. The OpenAI path keeps them as real messages in order (chat_prompt_builder.dart appends post-history at line 567, roadmap at 554, scene at 519-522). All four turn types are real and user-reachable (scene messages are user-creatable at chat_screen.dart:3598; post-history comes from any preset; guide/roadmap are toggleable features). No code re-positions them for Anthropic, so the reorder is guaranteed whenever any system-role turn other than the lead system prompt exists.
- **Veredito adversario:** Verified the mechanics, and they hold up — but the framing as a "bug" is partly a misframe; it's a forced design tradeoff with a soft, real-quality edge.

CONFIRMED FACTS (chat_prompt_builder.dart + chat_api.dart):
- buildChatPrompt emits post-history (line 567), Story Roadmap (line 554), and Scene messages (line 520) as ChatTurn('system', ...) placed AFTER/within the conversation history, with post-history deliberately last (recency-priority for the char-voice jailbreak).
- OpenAI path (buildRequestBody line 314 -> ChatTurn.toJson line 45) preserves these inline as {'role':'system'} at their exact chronological position.
- buildAnthropicBody (lines 390-396, 436) folds EVERY system-role turn

### [sync-server] _proxyBotbooru leaks raw upstream/internal exception text in 500 response bodies
- **Arquivo:** lib/services/pyre_server.dart:1033
- **Impacto:** Information disclosure: internal error details and the constructed upstream URL are surfaced to any (possibly unauthenticated) caller, unlike the other handlers which return generic `{"error":"server error"}` bodies (e.g. /pair line 274-275, /push line 687-688).
- **Repro:** Hit `/bbx/<path>` in a way that makes the upstream call throw or time out (e.g. botbooru.com unreachable, TLS failure, or the 30s timeout at line 1017 fires). The catch at lines 1031-1034 returns `Response.internalServerError(body: 'botbooru proxy error: $e')`, echoing the raw Dart exception (which can include the full target URL, socket/TLS error details, host info) straight to the caller — who, per the previous finding, need not be authenticated.
- **Por que e real:** The string interpolation `'botbooru proxy error: $e'` is in the source verbatim and is the response body on every proxy exception. Every other error path in this file deliberately returns a generic message; this one regressed from that pattern. Reachable by anyone who can reach /bbx/.
- **Veredito adversario:** I tried to refute this and could not make the core fact go away, but the claimed severity is overstated. VERIFIED FACTS: (1) `_proxyBotbooru` at C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\services\pyre_server.dart:1031-1033 does `return Response.internalServerError(body: 'botbooru proxy error: $e');`, echoing the raw Dart exception. (2) It is the ONLY error path in the file that interpolates `$e` into the body — every other handler returns generic `{"error":"server error"}`/`{"error":"no store"}` (lines 274-275, 301, 687-688, 750-796), so it is a genuine regression from the file's deliberate generic-error pattern. (3) `/bbx/` is genuinely unauthenticated: it is NOT in `_protected

### [import-export] Lightbox 'Save' and card-export gallery files write non-PNG image bytes under a .png name with image/png mime
- **Arquivo:** lib/services/image_export.dart:31
- **Impacto:** The exported/shared file has a .png extension and is shared as image/png while the bytes are actually JPEG/WEBP/GIF. Apps/sites that trust the extension or Content-Type (some uploaders, strict image validators) reject or mis-handle the file. Cosmetic on OSes that content-sniff, but a real mismatch on the export deliverable.
- **Repro:** Have a character/persona with a JPEG/WEBP/GIF gallery image (see bug above for how those enter the gallery). Either (a) open the gallery lightbox and tap Save — gallery_strip.dart L278-280 builds the filename as `<owner>_gallery_N.png` and saveImageBytesToExports always passes mimeType 'image/png' (image_export.dart L36/56) regardless of the real bytes; or (b) export the character as a card — characters_screen.dart L696 writes each gallery file as `..._gallery_N.png` from resolveAvatarBytes bytes that may be JPEG/WEBP.
- **Por que e real:** image_export.dart hardcodes 'image/png' (L36 downloadBytesToBrowser, L56 XFile mimeType) and the callers hardcode the .png filename suffix (gallery_strip.dart L279-280, characters_screen.dart L696) while the bytes come straight from resolveAvatarBytes with no format check. Confirmed gallery images can be non-PNG (gallery_import.dart _imageMime + gallery_editor_section.dart FileType.image).
- **Veredito adversario:** Confirmed after attempting refutation; no upstream guard normalizes gallery bytes to PNG. CHAIN VERIFIED: (1) Non-PNG bytes genuinely enter the gallery. gallery_import.dart (the flagship BotBooru import) magic-byte-validates PNG/JPEG/WEBP/GIF (_imageMime L77-106) and stores the bytes with their REAL detected mime (L65) — its own comment (L33) notes "a WebP thumbnail is far smaller", i.e. BotBooru gallery thumbs are routinely WEBP. gallery_editor_section.dart (_addImage L53-72) uses FilePicker FileType.image (accepts jpg/webp/gif) and stores raw bytes with the extension-derived mime (L68-69). (2) No transcode anywhere: AttachmentStore.store writes bytes verbatim (attachment_store.dart L99 wri

### [presets-memory] "Copy (editable)" loses injection depth when cloning a modular preset
- **Arquivo:** lib/screens/presets_screen.dart:509
- **Impacto:** The forked preset silently behaves differently from the original: a depth-injected jailbreak/reminder becomes post-history text. The original is intact (only the copy is wrong), so it is less severe than the edit path, but the user gets a copy that does not match what they copied — and may then make the copy active.
- **Repro:** 1. Have a modular preset with a depth block (e.g. an ST import with `injection_position: 1`, depth 4). 2. Open its kebab → Copy (editable). The clone's promptBlocks are built at lines 507-517 with `PromptBlock(id, name, content, enabled, role, position)` — `depth` omitted. 3. The copy's depth block now has depth:null and renders/behaves as an afterHistory block instead of a depth-4 injection.
- **Por que e real:** Same root cause as the edit-draft bug: the manual PromptBlock copy omits the `depth` field that assembly and insertDepthTurns rely on. Confirmed `depth` is the only field dropped in both copy constructors versus the model's full field set.
- **Veredito adversario:** CONFIRMED real after trying to refute it. The copy constructor in lib/screens/presets_screen.dart:509-516 builds each cloned PromptBlock with (id, name, content, enabled, role, position) and omits `depth`. `PromptBlock.depth` is a real, separate field (lib/models/models.dart:1212, default null) that drives assembly: in lib/services/preset_assembly.dart:82, `if (b.depth != null)` routes the block to `depthTurns` (in-chat injection), otherwise it falls through to the before/after-history text/turn slots. The cloned block therefore drops to depth:null and is classified as an afterHistory block — and lib/services/chat_prompt_builder.dart:540 splices depthTurns INTO the replayed history via inser

### [presets-memory] Out-of-range memoryLimit/autoEvery crashes the Checkpoints Slider (no M-5 clamp on the private _SliderCard)
- **Arquivo:** lib/screens/long_term_memory_screen.dart:332
- **Impacto:** In debug the assert throws and the Checkpoints screen fails to render; in release the thumb renders out of bounds / odd. The screen becomes unusable until the value is brought back in range by other means.
- **Repro:** Get a MemorySettings whose `memoryLimit` is outside [100,5000] or `autoEvery` outside [0,60] — e.g. via LAN sync of memorySettings from a build with different bounds, a restored/older backup, or a hand-edited settings JSON (MemorySettings.fromJson at models.dart:2094-2095 reads the raw int with `_jInt`, which does NO clamping). Open More → Checkpoints. The private `_SliderCard` passes `value: value` straight into Material `Slider(value:, min:, max:)` at line 331-332 with no clamp, violating Slider's `min <= value <= max` assert.
- **Por que e real:** The codebase already recognized this exact failure mode: the shared SliderCard widget added `_clampedSliderValue` (setting_slider.dart:119, the M-5 fix) precisely because "a synced/hand-edited backup with maxTokens > 4096" can exceed slider bounds. This private `_SliderCard` in long_term_memory_screen was not given the same clamp, and memorySettings IS synced (SYNC W3). _jInt confirmed to do no clamping (models.dart:22-27).
- **Veredito adversario:** CONFIRMED after trying to refute. The private _SliderCard in long_term_memory_screen.dart:331-332 passes `value: value` straight into Material `Slider(value:, min:, max:)` with no clamp, unlike the shared SliderCard (setting_slider.dart:257) which uses `_clampedSliderValue` (the M-5 fix). I traced every ingestion path and found no upstream guard: MemorySettings.fromJson (models.dart:2082-2101) reads autoEvery/memoryLimit via `_jInt` (models.dart:22-27) with NO clamping; all three loaders call it raw — normal load (app_store.dart:668), sync apply (app_store.dart:3187-3188), and backup restore (backup_restore_screen.dart:1013-1016) — and updateMemorySettings (app_store.dart:3299) doesn't clamp

### [presets-memory] parseRegexLiteral mis-splits a path-like pattern that ends in slash+letters
- **Arquivo:** lib/services/regex_rules.dart:397
- **Impacto:** A rule whose intended pattern is a slash-delimited string ending in letters gets a truncated pattern, so it matches the wrong text (or fails to match). Affects user-typed Find input and ST regex-script import (parseStRegexScript → parseRegexLiteral).
- **Repro:** In the regex rule "Find" box (or an ST import's findRegex), enter `/a/b/c` intending the literal pattern. parseRegexLiteral takes lastIndexOf('/') (before `c`), so body=`a/b`, flags=`c`. The check `RegExp(r'^[a-zA-Z]*$').hasMatch('c')` is true and body is non-empty, so it returns (pattern: 'a/b', flags: 'c') — the pattern is silently changed from `/a/b/c` to `a/b` and a bogus flag `c` (ignored).
- **Por que e real:** The inline comment at lines 394-396 explicitly claims this case stays "usable verbatim when 'b/c' isn't a flag run" — but the code only inspects the segment after the LAST slash (`c`), not `b/c`, so the claim is false and the split fires. Reachable via any pattern matching the shape `/<non-empty>/<letters>`.
- **Veredito adversario:** CONFIRMED real after trying to refute it. Trace of parseRegexLiteral('/a/b/c') at lib/services/regex_rules.dart:387-403: lastIndexOf('/')=4 → body='a/b', flags='c'; the guard RegExp(r'^[a-zA-Z]*$').hasMatch('c') is true and body non-empty → returns (pattern:'a/b', flags:'c'). The intended literal '/a/b/c' is silently truncated to 'a/b' with a bogus ignored flag 'c'. The existing test (test/regex_rules_test.dart:263-270) asserts exactly this output, so the behavior is codified, not accidental.

Reachable via TWO real entry points, both passing raw text straight in: (1) user-typed Find box — regex_rules_screen.dart:297 (live preview) and :322 (_save); (2) ST regex-script import — parseStRegexS

### [state-models] A single wrong-typed scalar field in the state blob throws out of AppStore.load() and crashes app startup (defeats salvage/backup recovery)
- **Arquivo:** C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\state\app_store.dart:617
- **Impacto:** The Wave CY.18.40 hardening isolates per-MODEL parse failures via _parseList/_parseObject (each wraps parse in try/catch), but the ~30 scalar reads at the top of load() are unguarded direct casts: `(raw['seenOnboarding'] as bool?)` (line 617), `raw['activeProviderId'] as String?` (539), `raw['activePersonaId'] as String?` (579), `raw['dismissedUpdateVersion'] as String?` (618), etc. In Dart, `1 as bool?` / `42 as String?` throw TypeError. The exception propagates out of load(), main()'s future rejects, runApp is never called, and the app dies on a blank screen with no recovery — and the storage-layer salvage/rotated-backup machinery (storage.dart) can't help because the JSON parsed fine; the crash is in AppStore field extraction, not JsonStorage. `as num?` tolerates int-vs-double so those are safe; the danger is genuine type-category mismatch.
- **Repro:** Have the on-disk state blob (or a restored/hand-edited backup, or an old JS-prototype export) contain a top-level scalar with the wrong JSON type — e.g. seenOnboarding as a number/string (`"seenOnboarding": 1`), or activePersonaId/activeProviderId/dismissedUpdateVersion as a number. On native, main() (main.dart:175) does `await store.load()` with NO try/catch and main() is a plain async function (no runZonedGuarded); web hits the same at main.dart:281.
- **Por que e real:** Confirmed load() (lines 481-977) has no enclosing try/catch around the scalar reads — only the _parseList/_parseObject helpers and the secure-key hydration loop are guarded. Confirmed main() awaits store.load() bare (main.dart:175/281) and is not wrapped in runZonedGuarded; ErrorLog.install() only hooks FlutterError/PlatformDispatcher, which do not catch a pre-runApp awaited throw. The codebase explicitly supports JS-prototype/hand-edited backups (storage.dart and load() comments), the realistic source of mistyped scalars, so this is reachable via restore.
- **Veredito adversario:** VERIFIED, but narrower than the claim frames it. The code-shape facts all check out: lib/state/app_store.dart load() (481-977) reads ~30 top-level scalars as UNGUARDED direct casts — line 617 `seenOnboarding = (raw['seenOnboarding'] as bool?) ?? false`, 539/579/618 `... as String?`, etc. — with NO enclosing try/catch (only _parseList/_parseObject and the secure-key loop are guarded). main() (main.dart:175, web 281) awaits store.load() bare with no runZonedGuarded; ErrorLog.install() only hooks FlutterError.onError + PlatformDispatcher.onError (the "(c) runZonedGuarded" comment is aspirational — no zone exists), neither of which catches a synchronous throw out of main's future before runApp. 

### [web-paths] ?import= web bookmarklet handoff is dead code — unreachable on web, the only platform it targets
- **Arquivo:** lib/main.dart:261
- **Impacto:** The web `?import=` bookmarklet card-import feature never fires on web. A user who clicks the bookmarklet / opens an `?import=` link in the web app sees the app load with no import, no dialog, no error. The entire trusted-host allowlist (_trustedImportHosts), SSRF guard (_fetchCardCapped no-redirect + 25MB cap), and confirm dialog built for this path are dead weight.
- **Repro:** On web, main() returns early at lines 151-158: if (kIsWeb) { if (!isPaired) {runApp(WebPairFirstApp); return;} await _bootWebApp(); return; }. Both web branches return before control ever reaches the `if (kIsWeb) { _maybeHandleImportParam(store); }` block at lines 261-263. That block lives in the NATIVE continuation of main() (after the web early-return), where kIsWeb is always false. _bootWebApp() (lines 279-284) — the actual web boot path — never calls _maybeHandleImportParam. So: visit `https://<pyre-web>/?import=https://botbooru.com/...` (the documented bookmarklet hand-off). Result: nothing imports. To trace: grep shows _maybeHandleImportParam has exactly ONE caller (main.dart:262), and it sits past the web return.
- **Por que e real:** Pure control-flow: the web early-return at main.dart:151-158 unconditionally precedes line 261, and the kIsWeb guard at 261 is permanently false in the native continuation. _bootWebApp omits the call entirely. Verified _maybeHandleImportParam has a single caller via grep. The code's own comments (lines 258-260, 355-359, 447) state this is a WEB feature, confirming intent vs. actual reachability mismatch — not dead-by-design.
- **Veredito adversario:** Confirmed real after tracing actual control flow in lib/main.dart. The web early-return is structural: at lines 151-158, both web sub-paths return before line 261 — `if (kIsWeb) { if (!isPaired) { runApp(WebPairFirstApp); return; } await _bootWebApp(); return; }`. The `?import=` handler at lines 261-263 (`if (kIsWeb) { _maybeHandleImportParam(store); }`) lives in the native continuation of main(), so any execution reaching it has kIsWeb==false; the guard is permanently false and the block is dead. grep confirms _maybeHandleImportParam (defined line 341) has exactly ONE caller (line 262). The real web boot path, _bootWebApp() (lines 279-284), does NOT call it — verified by reading the functio

### [web-paths] AvatarBubble negative-decode cache branch is unreachable; malformed/failed avatar data URLs re-decode every rebuild (streaming flicker/perf)
- **Arquivo:** lib/widgets/avatar.dart:82
- **Impacto:** During token streaming the chat tree rebuilds many times per second (the exact scenario the cache header comment cites). Any message/avatar with a broken data: URL pays a fresh failed base64 decode each frame instead of a cached no-op — wasted CPU and the avatar-flicker the cache was specifically built to prevent.
- **Repro:** _decodeAvatar caches into Map<String, MemoryImage> (non-nullable value). The 'cached failure' branch at lines 82-85 (`if (_avatarDecodeCache.containsKey(dataUrl)) return null;`) can only be reached when the map holds the key — but a present key always has a non-null MemoryImage, so line 81 (`if (cached != null) return cached;`) already returned. The branch is provably dead. Meanwhile the real failure paths never populate the cache: the malformed-prefix path (lines 87-90) adds then immediately removes the key, and the base64Decode catch (lines 100-102) returns null without caching. So a character whose `dataUrl` is a malformed/undecodable data: URL re-runs indexOf + base64Decode on EVERY build().
- **Por que e real:** Type-level proof: the map value type MemoryImage is non-nullable, so the containsKey-based negative cache at lines 82-85 can never observe a key whose decode failed; the failure paths at 87-90 and 100-102 deliberately don't cache. The dead branch + non-caching failure paths together mean repeated decode attempts are guaranteed for any un-decodable avatar string that survives across rebuilds.
- **Veredito adversario:** The code-level claim is accurate. In lib/widgets/avatar.dart:79-103 (_decodeAvatar): the map _avatarDecodeCache is Map<String, MemoryImage> (non-nullable value), so line 80 (_avatarDecodeCache[dataUrl]) returns null ONLY when the key is absent; therefore line 81's `cached != null` already returns for any present key, and the negative-cache branch at lines 82-85 (`if (containsKey) return null`) is provably dead/unreachable. The two real failure paths confirm the rest: the no-comma path (lines 87-90) does add-then-immediately-remove (a caching no-op), and the base64Decode catch (lines 100-102) returns null WITHOUT caching. So a `data:` avatar whose base64 payload is undecodable re-runs base64D

### [ui-widgets] _SliderRow in Customize Chat feeds unclamped ChatSettings values to Material Slider (assert/crash in debug, mis-rendered thumb in release)
- **Arquivo:** C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\screens\chat_appearance_screen.dart:724
- **Impacto:** In a debug build this trips Flutter's assert(value>=min && value<=max) inside Slider and throws, breaking the whole Customize Chat screen. In release the assert is stripped but the thumb fraction (value-min)/(max-min) lands outside [0,1], drawing the thumb off-track/clipped. The user can't open the appearance screen (debug) or sees a broken control (release).
- **Repro:** 1) Obtain a ChatSettings JSON whose bubbleTextScale / bubbleBlurSigma / bubbleCornerRadius / bubbleBorderWidth is outside the slider's [min,max] (e.g. a hand-edited backup with {"bubbleTextScale": 2.0} or {"bubbleBlurSigma": 20}, or a blob synced from a build with different caps). 2) Restore/load it: ChatSettings.fromJson at models.dart:2592-2597 stores the raw value with NO clamp. 3) Open More -> Customize Chat. The 'Bubble text size' / 'Background blur' / 'Corner radius' / 'Border width' rows build Slider(value:<raw>, min:.., max:..) via _SliderRow (lines 724-732), where value is passed through verbatim.
- **Por que e real:** Confirmed by contrast: every OTHER deserialized-value slider in the app clamps before Slider. SliderCard uses _clampedSliderValue (setting_slider.dart:119-120, the explicit M-5 fix); display_settings_screen uses store.uiPrefs.clampedUiScale (line 117); customize_chat_sheet clamps with effectiveOpacity.clamp(0.0,1.0) (line 606). Only _SliderRow (chat_appearance_screen.dart:724) passes value straight through with no clamp, and ChatSettings.fromJson (models.dart:2592-2597) applies no range clamp on load. The hand-edited/cross-version backup vector is exactly the scenario the M-5 SliderCard fix was written for, so the out-of-range path is real and was already hit elsewhere.
- **Veredito adversario:** The claim holds up after adversarial tracing. CONFIRMED PATH: ChatSettings.fromJson (lib/models/models.dart:2592-2597) reads bubbleCornerRadius/bubbleBorderWidth/bubbleBlurSigma/bubbleTextScale via (j[...] as num?)?.toDouble() ?? default with NO clamp. This is reachable from three real entry points that consume user/peer-controlled JSON and assign the result to the live chatSettings with no clamp anywhere downstream: backup restore (lib/screens/backup_restore_screen.dart:1011), sync pull (lib/state/app_store.dart:3200), and on-disk load (app_store.dart:666). updateChatSettings (app_store.dart:3293) also does not clamp. ChatAppearanceScreen.initState (lib/screens/chat_appearance_screen.dart:4

### [ui-widgets] _BackdropImage re-decodes raw-base64 backdrops on every rebuild (per-frame base64Decode + new MemoryImage during streaming -> flicker/jank)
- **Arquivo:** C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\screens\chat_screen.dart:5204
- **Impacto:** Lightbox.resolveImage's raw-base64 branch (lightbox.dart:70-75) runs base64Decode + new MemoryImage(...) on every build, producing a fresh ImageProvider identity each frame. Flutter sees a new image key and re-decodes/re-uploads the texture, so the backdrop visibly flickers and adds decode jank for the whole streaming duration -- the exact failure the data:-path _decode() cache (lines 5130-5162) was built to prevent.
- **Repro:** 1) Import a legacy chub.ai card / hand-edited backup whose character (or persona) avatar is stored as RAW base64 with no data: prefix (lightbox.dart:62-69 documents this exact format existing in the wild). 2) Open a chat with that character using the default background source (characterAvatar); _resolveBackdrop returns the raw-base64 string (chat_screen.dart:326). 3) Send a message and watch the reply stream. _BackdropImage rebuilds every coalesced frame (same Stack as the chat; comment lines 5184-5186 confirm 'rebuilds on every streamed chunk / keystroke'). The data: branch uses the cached _decode(), but a raw-base64 string is not data:/asset:, so it falls to line 5204 Lightbox.resolveImage(dataUrl).
- **Por que e real:** The _decode() cache is only consulted in the dataUrl.startsWith('data:') branch (line 5187). The raw-base64 (no-scheme) value reaches the fallthrough Lightbox.resolveImage branch (5204), and resolveImage's final clause (lightbox.dart:70-75) constructs a brand-new MemoryImage with no caching. _resolveBackdrop returns raw character.avatar for the default characterAvatar source (chat_screen.dart:326), and raw-base64 avatars are explicitly an existing data shape per the lightbox comment, so this is reachable, not hypothetical.
- **Veredito adversario:** The code-level mechanism is real and I could not refute it: _BackdropImage's _decode() cache (chat_screen.dart:5137-5162) is only consulted in the data: branch (5187). A no-scheme raw-base64 string falls through to line 5204 -> Lightbox.resolveImage's raw-base64 clause (lib/widgets/lightbox.dart:70-75), which on EVERY call does base64Decode -> a fresh Uint8List -> a new MemoryImage. MemoryImage equality is keyed on the bytes-object identity, so each streaming-rebuild produces a new image key and Flutter re-decodes/re-uploads the texture. _BackdropImage rebuilds per coalesced chunk (comment 5184-5186 confirms). I tried hard to refute it: (1) the load-time migration attachment_migration.dart:4

### [ui-widgets] _decodeAvatar 'cached failure' branch is dead code -- malformed data: avatars re-run base64Decode on every rebuild
- **Arquivo:** C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\widgets\avatar.dart:100
- **Impacto:** The decode failure is never memoized, so every rebuild re-attempts base64Decode on the bad payload -- wasted CPU per frame for as long as the broken avatar is on screen. The comment at lines 82-85 ('Cached failure -- don't keep retrying a broken URL') promises this won't happen, but the guard is unreachable.
- **Repro:** 1) Have a character/persona whose avatar is a data: URL with a valid comma but corrupt/truncated base64 payload (e.g. a partially-synced or hand-edited card). 2) Open a chat or list where that AvatarBubble is visible. 3) During streaming or scrolling the bubble rebuilds repeatedly; each rebuild calls _decodeAvatar, which on a malformed payload throws inside base64Decode (line 93), is caught at line 100, and returns null WITHOUT writing anything to the cache.
- **Por que e real:** Failures are never persisted: the only write that could mark a failure (lines 88-89) inserts a MemoryImage then IMMEDIATELY removes it, and the catch at line 100 returns null without touching the cache. Therefore _avatarDecodeCache.containsKey(dataUrl) at line 82 is always false for a failed URL, so the early-return at 84 is dead and the decode is retried every call. Low severity because base64Decode fails fast and broken avatars are uncommon, but it is a genuine defeated-intent bug with a real per-frame cost.
- **Veredito adversario:** I tried to refute this and could not. The code at C:\Users\Gui\Desktop\BotBooru chat app\flutter_app\lib\widgets\avatar.dart:79-103 behaves exactly as claimed.

KEY PROOF (the cached-failure branch is unreachable): `_avatarDecodeCache` is typed `Map<String, MemoryImage>` (line 66) — a NON-NULLABLE value type. The only writes are line 88-89 (insert MemoryImage(Uint8List(0)) then IMMEDIATELY .remove() → net key absent) and line 98 (insert a real non-null image). No code path ever maps a key to a null value. Therefore line 80's `_avatarDecodeCache[dataUrl] == null` (cached==null) and line 82's `containsKey(dataUrl)==true` are mutually exclusive — they can never both hold. The early-return at li

---
## FIX LOG
- 2026-06-13: HIGH #3 (presets_screen:917) + LOW #14 (presets_screen:509) — block clone now copies `depth: b.depth` (both editor-draft + Copy-editable). Add-block site (1268) confirmed correct (fresh block = null depth). analyze clean + depth tests 15/15 green. CEO-verified + fixed directly (trivial, my own session regression).

- 2026-06-14: WAVE 2 (perf + new-feature correctness + robustez + OOC feature + haptics). 4 disjoint ember-implementer slices → central green gate (analyze 0 + **1495 tests**) → CEO read the 2 biggest diffs → independent ember-verifier PASS on all 4. Closed:
  - **Perf / streaming flicker:** LOW avatar.dart negative-cache (dead branch → `Map<String,MemoryImage?>` memoizes failures; covers both avatar.dart:82 & :100 dupes) + LOW backdrop/raw-base64 re-decode (lightbox.dart `resolveImage` now returns a STABLE bounded-cached MemoryImage → fixes the per-frame flicker the chat backdrop hit via the shared helper).
  - **Anthropic adapter (new 1.1.3 feature):** MEDIUM #1 (chat_api.dart:536) — `_streamAnthropic` + `_completeAnthropic` now do the universal retry-without-extras exactly once on a param-error 4xx (via new `minimalAnthropicRetryBody`), matching the OpenAI backstop; LOW #1 (chat_api.dart:388) — `buildAnthropicBody` keeps LEADING system in top-level `system` but preserves POST-HISTORY system turns (jailbreak/scene/roadmap/guide) IN ORDER as user-role turns (role-alternation safe). Updated the one pre-existing test that codified the old folding bug.
  - **Robustez:** LOW app_store.dart:617 (tolerant `_jStr`/`_jBool` on ~17 load() scalars → no startup crash on mistyped backup); MEDIUM #5 lorebook_import.dart:161 (tolerant casts: 0/1→bool, numeric strings→num, never throws on off-spec entry); MEDIUM #6 characters_screen.dart:653 (transcode non-PNG avatar→PNG before encodeCharaCardPng, both char + persona, clean error on undecodable); LOW slider clamps (long_term_memory_screen.dart:332 + chat_appearance_screen.dart:724, display-only clamp); MEDIUM #7 attachment_refs.dart:71 (GC referenced-set now includes chat.characterSnapshots avatar/avatarOriginal/gallery — additive).
  - **OOC + Scene = real messages (founder decision, NOT one of the 22):** `isReadOnlyAuxKind` is now system-ONLY; OOC/Scene render user-side with an OOC/Scene label, support edit/delete/branch(+)/swipe; regenerate stays char-only. Mirrored in chat_tree_screen.dart. New test/ooc_message_behavior_test.dart.
  - **Haptics (founder-approved 'alma' item):** mobile-gated `HapticFeedback` on send / branch-add-variant / regenerate / long-press menu / variant swipe (no-op on desktop/web).
  - STILL UNFIXED (deferred, by judgment): MEDIUM #4 /bbx unauth endpoint + LOW /bbx error leak (LAN security — handle with care, see the FLAGGED-for-Kuru LAN items); LOW gallery png-mime (cosmetic); LOW regex parse path-like edge (regex_rules.dart:397); LOW web `?import=` dead code (main.dart:261). NOT yet committed — awaiting Kuru's checkpoint OK. NOT runtime-tested on the live GUI (the hands-on visual pass Kuru gated to "after we fix these points").
